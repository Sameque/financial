<%= form_with(model: payment, local: true) do |form| %>
  <% if payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :payment_name %>
  </div>

  <div class="field">
    <%= form.label :money %>
    <%= form.check_box :money, id: :payment_money %>
  </div>

  <div class="field">
    <%= form.label :card %>
    <%= form.check_box :card, id: :payment_card %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
