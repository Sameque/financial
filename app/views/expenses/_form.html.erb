<%= form_with(model: expense, local: true) do |form| %>
  <% if expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :expense_number %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value, id: :expense_value %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :expense_description %>
  </div>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references, id: :expense_references %>
  </div>

  <div class="field">
    <%= form.label :paid %>
    <%= form.check_box :paid, id: :expense_paid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
